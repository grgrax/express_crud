extends ../layout

block content
  a(href="/bank", class="btn btn-default") Banks
  h2= title
  form(action="", method="post", )
   .form-group
    label Name
    input(name="name", class="form-control", value=bank.name)
   .form-group
    label Url
    input(name="url", class="form-control", value=bank.url)
   input(name="submit", type="submit", class="btn btn-default")

  